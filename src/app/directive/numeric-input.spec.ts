import { NumericInput } from './numeric-input';

describe('NumericInput', () => {
  it('should create an instance', () => {
    const directive = new NumericInput();
    expect(directive).toBeTruthy();
  });
});
